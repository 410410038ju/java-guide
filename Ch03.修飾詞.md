# Ch3. 修飾詞

<!-- TOC -->

- [Ch3. 修飾詞](#ch3-修飾詞)
  - [修飾詞 (Modifiers)](#修飾詞-modifiers)
    - [存取修飾詞（Access Modifiers）](#存取修飾詞access-modifiers)
    - [非存取修飾詞 (Non-access Modifiers)](#非存取修飾詞-non-access-modifiers)
  - [重點整理](#重點整理)
  <!-- TOC -->

## 修飾詞 (Modifiers)

修飾詞用來控制類別、屬性、方法等的訪問權限和行為。主要分為 `存取修飾詞` 和 `非存取修飾詞`
，它們可以影響代碼的可見性、繼承性和執行方式。

### 存取修飾詞（Access Modifiers）

存取修飾詞控制成員的可見性和訪問範圍，包括 `public`、`protected`、`default`、`private`，來達到`封裝`概念。

| 修飾詞         | 訪問範圍                     | 用於類別/方法/屬性 |
| -------------- | ---------------------------- | ------------------ |
| `public`       | 任何類別都能訪問             | `類別、屬性、方法` |
| `protected`    | 同一包內、以及子類別可以訪問 | `屬性、方法`       |
| `默認(無修飾)` | 同一包內可以訪問             | `類別、屬性、方法` |
| `private`      | 只能在同一類別內訪問         | `屬性、方法`       |

**權限關鍵字與範圍**

| 關鍵字 | 類別內部 | 相同套件類別 | 不同套件類別 |
| ----- | --------- | ---------- | ----------|
| `public` | 可存取 | 可存取 | 可存取 |
| `protected` | 可存取 | 可存取 | 子類別可存取 |
| 無     | 可存取 | 可存取 | 不可存取 |
| `private` | 可存取 | 不可存取 | 不可存取 |

**範例 :**

```java
public class Example {

  public int publicField;
  protected int protectedField;
  private int privateField;
  int defaultField; // default

  public void publicMethod() {
  }

  protected void protectedMethod() {
  }

  private void privateMethod() {
  }

  void defaultMethod() {
  } // default
}
```

### 非存取修飾詞 (Non-access Modifiers)

非存取修飾詞用於設定類別或方法的特定屬性，通常控制行為或優先級，包含以下常見修飾詞：

| 修飾詞         | 功能                                     | 用於類別/方法/屬性 |
| -------------- | ---------------------------------------- | ------------------ |
| `static`       | 表示靜態成員，屬於類別而非物件           | 屬性、方法         |
| `final`        | 表示不可變或不可覆蓋                     | 類別、屬性、方法   |
| `abstract`     | 表示抽象類別或方法，無具體實現           | 類別、方法         |
| `synchronized` | 表示同步方法，在多執行緒環境下互斥執行   | 方法               |
| `volatile`     | 表示多執行緒共享變數需及時刷新，避免快取 | 屬性               |
| `transient`    | 表示屬性在序列化過程中被忽略             | 屬性               |
| `native`       | 表示該方法由本地代碼實現（非 Java 代碼） | 方法               |
| `strictfp`     | 強制使用 IEEE 754 標準進行浮點數運算     | 類別、方法         |

### final

final 可以用在：

- 變數（含參數、欄位、區域變數）
- 方法
- 類別

1. final 變數（field / 參數 / 區域變數）
   🧠 只能被賦值一次，之後不可更改。

**範例 :**

```java
final int number = 5;
number = 10; // ❌ 錯誤：無法重新指定
```

**但可以在建構子中賦值：**

```java
public class MyClass {
    final int x;
    MyClass() {
        x = 100; // ✅ 這是合法的
    }
}
```

**陣列與物件也能用 final，但注意：**

```java
final int[] arr = {1, 2, 3};
arr[0] = 99;     // ✅ 可以改內容
arr = new int[5]; // ❌ 不可以改參考

final StringBuilder sb = new StringBuilder("Hi");
sb.append("!");   // ✅ 可以改內容
sb = new StringBuilder(); // ❌ 不可以改參考
```

2. final 方法
   🧠 不能被子類別覆寫（override）

```java
class Parent {
    final void sayHi() {
        System.out.println("Hi!");
    }
}

class Child extends Parent {
    void sayHi() {        // ❌ 錯誤：無法 override
        System.out.println("Hello!");
    }
}
```

3. final 類別
   🧠 不能被繼承

```java
final class Animal {
    void sound() {
        System.out.println("Roar");
    }
}

class Dog extends Animal { // ❌ 錯誤：不能繼承 final 類別
}
```

#### ✨ 小技巧 / 小提醒
| 類型 | `final` 意義 |
| -- | ---------- |
| 變數 | 賦值後不可更改 |
| 方法 | 不能被覆寫 |
| 類別 | 不能被繼承 |

- final 加在變數上：只能賦值一次。
- final 加在方法上：不能被子類別 override。
- final 加在類別上：不能被繼承。

### static

static 可以用在：
- 變數（欄位）
- 方法
- 區塊
- 巢狀類別

1. static 變數（也叫「類別變數」）
🧠 屬於整個類別共享的變數，不是物件自己的。

```java
public class Student {
    static int totalStudents = 0; // 所有學生共用一個

    String name;

    Student(String name) {
        this.name = name;
        totalStudents++;
    }
}
```
**使用方法：**
```java
Student.totalStudents  // 透過類別名稱存取，不用 new 物件
```

2. static 方法（類別方法）
🧠 可以直接用「類別名稱」呼叫，不需要物件。
```java
public class MathTool {
    static int square(int x) {
        return x * x;
    }
}
```
**使用方式：**
```java
int result = MathTool.square(5); // 25
```
❗ 注意：static 方法裡面不能直接使用非 static 的成員

3. static 區塊（靜態初始區塊）
🧠 用來在類別第一次載入時初始化資料，只執行一次。
```java
public class Example {
    static int value;

    static {
        value = 42;
        System.out.println("類別載入時執行");
    }
}
```

4. static 巢狀類別（static nested class）
🧠 巢狀類別中，如果加上 static，就不需要外層類別的物件就能用。
```java
public class Outer {
    static class Inner {
        void sayHello() {
            System.out.println("Hello from static nested class");
        }
    }
}

// 使用方式：
Outer.Inner inner = new Outer.Inner();
inner.sayHello();
```

#### ✨ static 用法整理表
| 用法          | 說明                  |
| ----------- | ------------------- |
| `static` 變數 | 所有物件共用，存在記憶體一份      |
| `static` 方法 | 不用 new 就能呼叫         |
| `static` 區塊 | 類別載入時會執行一次          |
| `static` 類別 | 不需外部類別實體，也能建立巢狀類別物件 |

### static（靜態）和 非 static（實例/物件方法）的差別

#### 1. static 方法 / 變數（靜態）

- 屬於類別本身，不是屬於某個物件的。
- 可以在不建立物件的情況下，直接透過類別名稱或 main 方法呼叫。
- 常用於工具方法（像是 Math.pow()）或程式入口（main 方法就是 static）。

- 特點
  - 只能直接使用 static 變數或方法，不能直接使用非 static 成員（因為沒有特定物件）。
  - 靜態方法中不能用 this。

**範例 :**

```java
public class Example {
    public static void staticMethod() {
        System.out.println("這是靜態方法");
    }

    public static void main(String[] args) {
        Example.staticMethod();  // 不用建立物件，直接呼叫
    }
}
```

#### 2. 非 static 方法 / 變數（實例成員）

- 屬於物件本身，必須先建立物件才能使用。
- 每個物件擁有自己獨立的非 static 變數和方法。

- 特點

  - 可以存取該物件的所有成員（static 和非 static）。

  - 需要建立物件來呼叫。

**範例 :**

```java
public class Example {
    public void instanceMethod() {
        System.out.println("這是實例方法");
    }

    public static void main(String[] args) {
        Example obj = new Example();  // 建立物件
        obj.instanceMethod();          // 用物件呼叫非 static 方法
    }
}
```

#### 簡單記憶法

| 分類            | static (靜態)                    | 非 static (實例方法/成員)      |
| --------------- | -------------------------------- | ------------------------------ |
| 屬於            | 類別本身                         | 物件本身                       |
| 呼叫方式        | 不需建立物件，直接用類別名稱呼叫 | 需要先建立物件後呼叫           |
| 使用限制        | 只能使用 static 成員             | 可使用 static 和非 static 成員 |
| 是否能用 `this` | 不行                             | 可以                           |

### final + static

- static：這個屬性/方法是屬於整個類別共享的。
- final：這個值一旦設定，就不能被改變。
- static final = 全部人共用，而且不能改變。這樣的變數通常被當作「常數（constant）」。

| 關鍵字            | 代表意思              |
| -------------- | ----------------- |
| `static`       | 屬於類別，不用 new 就能用   |
| `final`        | 值不能改變             |
| `static final` | **共用 + 不可變 → 常數** |


| 修飾詞            | 代表意義             | 常見使用情境             |
| -------------- | ---------------- | ------------------ |
| `final`        | **不能改變**         | 常數、不能 override 的方法 |
| `static`       | **不屬於物件、屬於類別本身** | 工具方法、共用變數          |
| `final static` | **全域且不可改變的常數**   | 常數設定（例如網址、參數上限）    |


## 重點整理

- 使用`存取修飾詞`來達到`封裝`概念，保護程式碼不被輕易呼叫、修改。
- 使用其他`包`的`類別`時，使用`import`提升可讀性。
- `static` 能透過類別名稱方式呼叫，無需實例化。
- `final` 使方法不能覆寫，變數值不能修改。
- `abstract` 抽象類別或方法，無具體實現，`interface`就是抽象。
- `synchronized` 用來控制執行續，來到執行續安全。
