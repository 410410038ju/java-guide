# 註解

* [註解（Annotation）](#註解annotation)
* [常用的Java標準註解](#常用的java標準註解)
* [自訂註解 & 元註解](#自訂註解--元註解)
    * [自訂註解](#自訂註解)
    * [元註解（Meta-Annotations）](#元註解meta-annotations)
* [框架中的註解（以Spring為例）](#框架中的註解以spring為例)
* [常見使用情境（什麼時候用final/static/enum等）](#常見使用情境什麼時候用finalstaticenum等)
* [重點整理](#重點整理)
* [補充：註解Annotation與Comment的差異](#補充註解annotation與comment的差異)

## 註解（Annotation）

- 註解就是寫給編譯器、工具或其他開發者看的標記，告訴它們「這段程式碼有什麼特殊含義」，不會直接改變程式邏輯，但會影響編譯或執行行為。
- 註解是給編譯器、工具或程式自己用的「標記」，
- 可以幫助檢查錯誤、提醒使用者、簡化開發。
- 不同註解有不同用途，Java標準庫和框架裡有很多。

## 常用的Java標準註解

1. `@Override`
    * 表示這個方法是覆寫（Override）父類別的方法。
    * 讓編譯器幫你檢查覆寫是否正確（方法名稱、參數、回傳型態）。
```java
class Parent {
    void greet() {
        System.out.println("Hello from Parent");
    }
}

class Child extends Parent {
    @Override
    void greet() {  // 加了 @Override，編譯器會檢查有無正確覆寫
        System.out.println("Hello from Child");
    }
}
```

2. `@Deprecated`
    * 表示這個方法、類別或欄位「已過時」，不建議再使用。
    * 使用這個標記的程式碼會在編譯時出現警告，提醒你要改用新的方法。
```java
@Deprecated
public void oldMethod() {
    // 這個方法不要用了
}
```

3. `@SuppressWarnings`
    * 用來告訴編譯器忽略特定的警告。
    * 例如你知道某段程式可能會有unchecked警告，但你有把握不會有問題，就可以加這個註解。
```java
@SuppressWarnings("unchecked")
public void someMethod() {
    // ...
}
```

4. `@FunctionalInterface`
    * 表示這個介面是函式介面，只能有一個抽象方法。
    * 常用在Java 8 Lambda表達式中。
```java
@FunctionalInterface
public interface MyFunction {
    void apply();
}
```

## 自訂註解 & 元註解

### 自訂註解

- 用`@interface定義一個新的註解類型。
- 可以定義屬性（參數）。

**範例：**

```java
public @interface MyAnnotation {
    String value();
    int number() default 0;  // 有預設值的屬性
}
```

### 元註解（Meta-Annotations）

- 用來描述註解本身的行為。

- 主要有：
    -  `@Retention`：註解保留的時間長度（SOURCE、CLASS、RUNTIME）
    - `@Target`：註解可用的程式碼位置（類別、方法、欄位等）
    - `@Documented`：表示會被javadoc紀錄
    - `@Inherited`：允許子類繼承父類的註解

**範圍：**
```java
import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME) // 執行期還有效
@Target(ElementType.METHOD)         // 只能用在方法上
public @interface MyAnnotation {
    String value();
}
```

## 框架中的註解（以Spring為例）

1. `@Autowired`
    * 自動注入Bean（物件）。
    * 省去自己new物件。

**範例：**
```java
@Autowired
private UserService userService;
```

2. `@Controller`、`@Service`、`@Repository`
    * 標記類別的角色。
    * Spring根據這些註解管理物件生命週期。

3. `@RequestMapping`、`@GetMapping`、`@PostMapping`
    * 設定HTTP請求路由。

**範例：**
```java
@GetMapping("/users")
public List<User> getUsers() {
    return userService.getAllUsers();
}
```

## 常見使用情境（什麼時候用final/static/enum等）

- `final` 表示不可改變（常用在變數、方法、類別上）
- `static` 表示屬於類別，不屬於物件（共用）
- `enum` 用來定義一組固定的常數（列舉）

## 重點整理

| 註解                     | 功能            | 何時用                 |
| ---------------------- | ------------- | ------------------- |
| `@Override`            | 覆寫父類別方法，檢查正確性 | 你想覆寫方法時             |
| `@Deprecated`          | 表示過時，不推薦使用    | 方法或類別不建議用時          |
| `@SuppressWarnings`    | 抑制編譯器警告       | 你知道會有警告但沒問題時        |
| `@FunctionalInterface` | 標示函式介面        | 寫函式介面要用Lambda時      |
| `@Retention`、`@Target` | 定義自訂註解行為      | 自訂註解時               |
| Spring註解               | 管理物件注入、路由等    | 使用Spring框架做Web或後端開發 |


## 補充：註解Annotation與Comment的差異

1. `//`、`/* */`、`/** */` 是「註解 Comment」
- 這些是寫給「人」看的，不會影響程式執行，讓開發者更容易了解程式碼內容。

✏️ 常見三種註解方式：
| 種類           | 說明                       | 範例             |
| ------------ | ------------------------ | -------------- |
| `//`         | 單行註解                     | `// 這是一個單行註解`  |
| `/* ... */`  | 多行註解                     | `/* 這是多行註解 */` |
| `/** ... */` | JavaDoc 註解，會被工具產生 API 文件 |                |

```java
/**
 * 這是 JavaDoc 註解
 * 可以產生說明文件
 */
```
✅ 用途：
- 提高可讀性
- 寫說明文字給其他人或未來的自己看
- JavaDoc 可產出文件（像官網API那種）

2. `@Override`、`@Deprecated` 等是「Annotation 註解」
- 這是 Java 語言的功能性標記，給「編譯器、虛擬機、框架」看的，會影響程式行為或提供結構資訊。

```java
@Override
public String toString() {
    return "Hello";
}
```
這樣做，編譯器會檢查你是否真的有正確覆寫方法，寫錯方法名就會報錯。

**差別整理總表：**

| 項目       | 註解（Comment）           | 註解（Annotation）              |
| -------- | --------------------- | --------------------------- |
| 寫法       | `//`、`/* */`、`/** */` | `@Override`、`@Deprecated` 等 |
| 給誰看      | 給「人類開發者」看             | 給「Java 編譯器、虛擬機、框架」看         |
| 影響執行     | ❌ 完全不影響程式邏輯           | ✅ 有可能影響執行流程或邏輯              |
| 用途       | 幫助理解程式碼、產生 JavaDoc 文件 | 提供額外資訊給工具或框架使用              |
| 是否需執行時保留 | ❌ 編譯後不保留              | ✅ 某些可在執行期使用（如 @Retention）   |

